# Dead Letters - Gemini API Proxy
# Cloudflare Workers Configuration

name = "dead-letters-api"
main = "index.js"
compatibility_date = "2024-01-01"

# Workers Paid plan gives you more requests, but Free tier works for testing
# usage_model = "bundled"

# Environment variables (non-secret, optional)
[vars]
# APP_TOKEN = "your-optional-app-token"  # Uncomment to require app authentication

# Secrets are added via CLI, NOT in this file:
# wrangler secret put GEMINI_API_KEY
# wrangler secret put APP_TOKEN  (optional)
